#include "robobrawlClock.hh"

void
roboClock::cleanup( )
{
  //active_mc.clear();
  //known_devices.clear();
  //
  //for ( auto device : components )
  //  device->clear();
  //
  //tms_context.clear();
}
